import 'package:flutter/material.dart';

void main() => runApp(const outreach());

class outreach extends StatelessWidget {
  const outreach({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}