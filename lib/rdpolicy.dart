import 'package:flutter/material.dart';

void main() => runApp(const randdpolicy());

class randdpolicy extends StatelessWidget {
  const randdpolicy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}