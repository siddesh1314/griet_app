import 'package:flutter/material.dart';

void main() => runApp(const codeofethics());

class codeofethics extends StatelessWidget {
  const codeofethics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}