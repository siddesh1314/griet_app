import 'package:flutter/material.dart';

void main() => runApp(const innovation());

class innovation extends StatelessWidget {
  const innovation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}