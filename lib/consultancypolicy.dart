import 'package:flutter/material.dart';

void main() => runApp(const consultancypolicy());

class consultancypolicy extends StatelessWidget {
  const consultancypolicy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}