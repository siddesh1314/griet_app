import 'package:flutter/material.dart';

void main() => runApp(const revenue());

class revenue extends StatelessWidget {
  const revenue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}